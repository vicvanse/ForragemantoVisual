/** Re-export — implementação em data-store (Supabase ou disco local). */
export {
  emailToFileKey,
  readParticipant,
  writeParticipant,
  saveParticipantSignature,
} from "@/lib/research/data-store";
